<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sv ="clr-namespace:SyminUI.Controls.Decorations"
                    xmlns:cvte ="clr-namespace:SyminUI.Convertor.Emboss"
                    xmlns:cvt="clr-namespace:SyminUI.Convertor">
    <Style TargetType="{x:Type sv:EmbossBorder}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Background" Value="{StaticResource Background.Main}"/>
        <Setter Property="Foreground" Value="{StaticResource Text.Main}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="EmbossRatio" Value="1"/>
        <Setter Property="LightBrush" Value="{StaticResource Shader.Light}"/>
        <Setter Property="ShadowBrush" Value="{StaticResource Shader.Shadow}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sv:EmbossBorder}">
                    <ControlTemplate.Resources>
                        <cvte:RatioToMargin x:Key="RTM"/>
                        <cvte:RatioToBlur x:Key="RTB"/>
                        <cvte:LightAndShadow x:Key="LAS"/>
                        <cvt:BoolToVisibility x:Key="BTV"/>
                        
                    </ControlTemplate.Resources>
                    <Grid>
                        <!--光影区域-->
                        <Border x:Name="shadowBorder"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Visibility="{TemplateBinding ShaderEnabled,Converter={StaticResource BTV}}"
                                Margin="{TemplateBinding EmbossRatio,Converter={StaticResource RTM}}">
                            <Border.Background>
                                <MultiBinding Converter="{StaticResource LAS}" Mode="OneWay">
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" 
                                             Path="LightBrush" Mode="OneWay"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" 
                                             Path="ShadowBrush" Mode="OneWay"/>
                                </MultiBinding>
                            </Border.Background>
                            <Border.Effect>
                                <BlurEffect Radius="{Binding RelativeSource={RelativeSource TemplatedParent}, 
                                    Path=EmbossRatio, Converter={StaticResource RTB},Mode=OneWay}"/>
                            </Border.Effect>
                        </Border>
                        <!--内容区域-->
                        <Border Background="{TemplateBinding Background}" Margin="4" 
                                CornerRadius="{TemplateBinding CornerRadius}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>