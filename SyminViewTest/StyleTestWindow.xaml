<Window x:Class="SyminViewTest.StyleTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SyminViewTest"
        xmlns:svd="clr-namespace:SyminUI.Controls.Decorations;assembly=SyminUI"
        mc:Ignorable="d"
        Title="Styles Demo" Height="450" Width="800"
        Style="{StaticResource Window.Normal}"
        xmlns:sve ="clr-namespace:SyminUI.Effects;assembly=SyminUI"
        >
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--菜单栏-->
        <Grid Height="32" VerticalAlignment="Top" Margin="0,-32,0,0">
            <Menu Background="Transparent" FontSize="14" Margin="2" Grid.Column="1"
                                  Foreground="{StaticResource Text.Main}"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  WindowChrome.IsHitTestVisibleInChrome="True">
                <MenuItem Header="File" >

                </MenuItem>

                <MenuItem Header="Edit">

                </MenuItem>
                <MenuItem Header="Help">

                </MenuItem>

            </Menu>

        </Grid>

        <StackPanel Orientation="Vertical">
            <Button Content="Button Basic"/>
            <Button Content="Button Basic Disabled" IsEnabled="False"/>
            <Button Content="Button Primary" 
                    Style="{StaticResource ButtonPrimary}"/>
            <Button Content="Button Primary Disabled" 
                    Style="{StaticResource ButtonPrimary}" IsEnabled="False"/>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Column="1">
            <Grid Height="120" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <svd:SlotBorder Content="Slot" VerticalContentAlignment="Center"
                                HorizontalContentAlignment="Center"
                                Foreground="{StaticResource PrimaryGradient}"
                                  FontSize="24" Radius="16"/>
                <svd:EmbossBorder Content="Emboss" VerticalContentAlignment="Center"
                                  HorizontalContentAlignment="Center" Grid.Column="1"
                                  Foreground="{StaticResource PrimaryGradient}"
                                  FontSize="24" CornerRadius="16"/>
                <Border Grid.Column="2" Margin="4" CornerRadius="16" BorderThickness="1"
                        BorderBrush="{StaticResource Border.Main.Shaded}"
                        Background="{StaticResource Background.Main}">
                    <TextBlock Text="Card" FontSize="24" HorizontalAlignment="Center"
                               VerticalAlignment="Center" Foreground="{StaticResource PrimaryGradient}"/>
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" Color="#B3B5C1" Opacity="0.6"/>
                    </Border.Effect>
                </Border>

            </Grid>
            <TextBox Text="Text Box (Input Field)"/>
            <ProgressBar Height="24" IsIndeterminate="{Binding ElementName=ProgressCheckbox,Path=IsChecked}"
                         Value="{Binding ElementName=progressSlider,Path=Value}"/>
            <Slider x:Name="progressSlider" Maximum="100" Minimum="0" Value="25"/>
            <CheckBox x:Name="ProgressCheckbox" Content="Progress Is Indeterminate"/>
            <Border Background="#FF256CEE" Width="200" Height="40" CornerRadius="16">
                <TextBlock Text="123123123123123" Foreground="{StaticResource Text.Secondary}" FontSize="18"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border.Effect>
                    <sve:LightedSurfaceEffect Intensity="0.4" LightSize="80"/>
                </Border.Effect>
            </Border>
            <svd:LightedSurface Width="300" Height="80" Background="AliceBlue"/>
        </StackPanel>
    </Grid>
</Window>
